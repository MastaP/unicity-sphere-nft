# Wallet opened in popup mode (standalone page, no Sphere extension).
# Unset or empty = production Sphere, https://sphere.unicity.network
# Other wallet builds:
#   VITE_WALLET_URL=https://sphere.staging.unicity.network            (staging)
#   VITE_WALLET_URL=https://unicity-sphere.github.io/sphere/<branch>  (Pages preview of a Sphere branch)
VITE_WALLET_URL=
